plugins {
    id("wheel").version("0-SNAPSHOT").apply(false)
}

group("user11681")
version("0.0.0")

allprojects {
    plugins.apply("wheel")

    dependencies {
        modInclude("api-base")
        modInclude("api-resource-loader")
    }
}

loom {
    accessWidener("src/main/resources/cell.accesswidener")
}
